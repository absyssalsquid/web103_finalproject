{
  "name": "bird-court",
  "version": "1.0.0",
  "description": "A community-driven web application where everyday objects are put on trial under an absurd legal system governed entirely by birds",
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && node --require dotenv/config server.js",
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && npm start\"",
    "seed": "cd server && npm run seed",
    "progress": "cd server && npm run progress"
  },
  "keywords": ["bird", "court", "legal", "game"],
  "license": "ISC"
}
